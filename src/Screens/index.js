import MainScreen from './Main';
import ZipScreen from './Zip';
import UnloadedScreen from './Unloaded';

const screens = {
  Main: MainScreen,
  Zip: ZipScreen,
  Unloaded: UnloadedScreen,
};

export default screens;
